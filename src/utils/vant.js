// vant组件按需引入
import Vue from "vue";

import { Toast, Dialog, Tabbar, TabbarItem, Grid, GridItem, Icon, Cell, CellGroup, List } from "vant";

Vue.use(Toast)
  .use(Dialog)
  .use(Tabbar)
  .use(TabbarItem)
  .use(Grid)
  .use(GridItem)
  .use(Icon)
  .use(Cell)
  .use(CellGroup)
  .use(List)
  ;
